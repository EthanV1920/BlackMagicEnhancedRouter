{
  "name": "@blackmagic-enhanced-router/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --out-dir dist --clean",
    "dev": "tsx watch src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@blackmagic-enhanced-router/shared": "workspace:*",
    "@blackmagic-enhanced-router/videohub-protocol": "workspace:*",
    "@fastify/static": "^8.3.0",
    "@fastify/websocket": "^11.2.0",
    "env-paths": "^3.0.0",
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "tsup": "^8.5.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}

